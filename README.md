# 🚌 Hokie Highways

## Overview

Hokie Highways is a web-based bus transit application designed to reimagine the public transit experience in Blacksburg. Built as a proof of concept leveraging the Blacksburg Transit API, this project was developed by Virginia Tech students for educational and experimental purposes only.

## 🚀 Running the Frontend

### Prerequisites

* Node.js installed (v20+ recommended)

### Steps

1. Navigate to the frontend directory:

    ```bash
    cd frontend
    ````

2. Install dependencies (only required once after cloning):

    ```bash
    npm install
    ```

3. Start the development server

    ```bash
    npm run dev
    ```

## ⚙️ Running the Backend

### Prerequisites

* Node.js installed (v20+ recommended)
* Dependencies installed (Express, etc.)

### Steps

1. Navigate to the backend directory:

    ```bash
    cd backend
    ```

2. Install dependencies (only required once after cloning):

    ```bash
    npm install
    ```

3. Start the backend server:

    ```bash
    node server.js
    ```
